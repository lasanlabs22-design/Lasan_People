# Where the Hono API (./backend) is running. Called server-side only — never exposed to the browser.
LASAN_API_URL=http://localhost:4000
# Must match APP_TIMEZONE in backend/.env
NEXT_PUBLIC_APP_TIMEZONE=Asia/Kolkata
